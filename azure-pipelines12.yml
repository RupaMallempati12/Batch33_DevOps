trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'

steps:
- script: |
    # Insert your build commands here
    # Example: npm install (for Node.js), make (for Makefile-based projects), etc.
  displayName: 'Build'

- script: |
    # Insert your publish commands here
    # Example: npm publish (for Node.js), docker push (for Docker images), etc.
  displayName: 'Publish'
